# QueryFlow Backend Environment Configuration Example
# Copy this file to '.env' and fill in your actual database credentials and server settings.

# PostgreSQL Connection settings
DB_URL=jdbc:postgresql://localhost:5432/queryflow
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Spring Boot Server settings
SERVER_PORT=8080
